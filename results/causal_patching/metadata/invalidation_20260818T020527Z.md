# Invalidation notice: causal run 20260818T020527Z

This run was stopped and excluded from scientific analysis. It restored the entire clean
residual-stream tensor into the corrupted run at each tested layer. In a deterministic transformer,
this structurally recreates clean downstream computation and therefore yields normalized recovery
near 1 at every layer rather than localizing causal influence.

No output was deleted. The retained 37 motivation and 1 empathy pair records are audit evidence
only. The corrected primary protocol restores only the residual vector at the final valid prompt
token. Corrected runs must use a new run ID and must not mix these invalidated records into
scientific summaries.
